# Runtime deps (also needed at test time since there's no separate requirements.txt yet)
# discord.py is NOT listed here — it requires a real Discord token to import cleanly.
# The test suite is structured to never import discord, store, or bot at the module level.

# YAML parsing (for data/strings.yaml)
pyyaml>=6.0

# Web framework — webui/auth.py exposes pure (testable) helpers alongside its
# FastAPI routes. Unlike discord.py, FastAPI imports cleanly with no token, so
# it belongs in the test env (it also lets the auth middleware be tested).
# python-multipart is required by FastAPI at import time for Form() routes.
fastapi>=0.109.0
python-multipart>=0.0.6

# Testing
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0

# Linting
ruff>=0.4
